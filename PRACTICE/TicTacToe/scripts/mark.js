class Mark { // a type (tip de data) that represents a move
    constructor(i, j, type) {//contructorul e o functie care este executata cand este creat obiectul
        this.i = i;
        this.j = j;
        this.type = type;//x or o
    }
}